spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/aidocs
    username: aidocs
    password: aidocs
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

storage:
  endpoint: http://localhost:9000
  region: ap-northeast-2
  access-key: aidocs
  secret-key: aidocs1234
  bucket: aidocs-documents
