# 기여 안내

이 문서는 저장소에 변경을 올리는 절차를 다룹니다. 개발 환경을 세팅하는 방법은 [개발 가이드](docs/DEVELOPMENT.md)에 있습니다.

---

## 브랜치

| 브랜치 | 용도 |
|---|---|
| `main` | 통합 브랜치 |
| `feat/*` | 기능 개발 |
| `fix/*` | 버그 수정 |
| `refactor/*` | 리팩터링 |
| `docs/*` | 문서 작업 |
| `chore/*` | 설정, 빌드, 의존성 |

작업 브랜치는 `main`에서 분기하고, 완료 후 PR로 `main`에 머지합니다.

---

## 커밋 메시지

제목은 `<type>: <subject>` 형식을 따릅니다. 바디는 자유롭게 작성합니다.

```
feat: 문서 업로드 API 구현
fix: 대용량 PDF 파싱 시 OOM 수정
docs: README 프로파일 구성 추가
refactor: DocumentService 책임 분리
test: 문서 검색 통합 테스트 추가
chore: Spring Boot 4.1.0 업그레이드
```

---

## Pull Request

본문에는 무엇을 왜 바꿨는지 적습니다. 설계를 두고 고민한 내용이나 대안을 검토한 과정은 코드 주석이 아니라 여기에 남깁니다. 주석은 코드가 무엇을 하는지에 집중합니다.

리뷰어가 알아야 할 것이 있으면 함께 적습니다. 실행 방법이 바뀌었다면 이전과 이후를, 검증하지 않은 부분이 있다면 무엇을 확인하지 않았는지를 밝힙니다.

코드 스타일은 [코드 컨벤션](docs/CODE_CONVENTIONS.md)을 따릅니다.

---

## 마이그레이션

공유 DB의 스키마를 바꾸는 변경은 팀 전체에 영향을 줍니다. **한 번 적용된 마이그레이션 파일은 수정하지 않습니다.** 수정하면 나머지 팀원 전원이 체크섬 불일치로 앱을 띄울 수 없게 됩니다.

적용 절차는 [개발 가이드의 마이그레이션 적용](docs/DEVELOPMENT.md#마이그레이션-적용)을 따릅니다.
